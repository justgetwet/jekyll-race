---
layout: post
title: "save races"
---

### 1. races for a month

レース結果を保存する SaveRace class

```python
# -*- coding: utf-8 -*-
import urllib.request
from bs4 import BeautifulSoup
import numpy as np
import pandas as pd
import re
from datetime import datetime
import pickle
import time
import random
import calendar

from autorace import ScrapeOddsPark

class SaveRace(ScrapeOddsPark):
  
    def OneRace2Sr(self, date: str, place: str, race: int) -> pd.Series:

    def OneDay2DF(self, year: int, month: int, day: int) -> pd.DataFrame:

    def OneMonth2Df(self, year: int, month: int) -> pd.DataFrame:
    
    def SaveOneMonth2Pickle(self, year: int, month: int) -> None:
				...          
        print(f"a dataframe is saved to the {filename}.pickle!")

        
if __name__=='__main__':
  
    s = SaveRace()
    # sr = s.OneRace2Sr("20210611", "飯塚", 12)
    # print(sr)
    # df = s.OneDay2DF(2021, 5, 1)
    # print(df)
    # df = s.OneMonth2Df(2021, 5)
    # print(df)
    s.SaveOneMonth2Pickle(2021, 5)
```



